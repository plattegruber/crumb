{
  "name": "crumb",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter api dev",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
