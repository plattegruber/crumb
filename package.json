{
  "name": "crumb",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter api dev",
    "test": "pnpm --filter api test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
